{
  "name": "gabriel-reviews-sync",
  "version": "2.0.0",
  "description": "Wöchentlicher Google Reviews Sync via GitHub Actions → public/reviews.json",
  "scripts": {
    "sync": "node scripts/syncOnce.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
